FROM ubuntu:24.04

# Android build sandbox — not implemented in this feature (placeholder).
# Future implementation will install Android SDK build tools,
# compile the submitted APK, and run instrumented tests via
# an AVD or connected emulator managed by sandbox-runner:executor.
